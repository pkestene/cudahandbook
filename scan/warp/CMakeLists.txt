# these test don't build because shfl_scan_add_step is missing


#build_exec(testScanBlock)
#target_include_directories(testScanWarp PUBLIC ../int)

#build_exec(testScanWarp)
#target_include_directories(testScanWarp PUBLIC ../int)
